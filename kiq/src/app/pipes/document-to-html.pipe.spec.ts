import { DocumentToHtmlPipe } from './document-to-html.pipe';

describe('DocumentToHtmlPipe', () => {
  it('create an instance', () => {
    const pipe = new DocumentToHtmlPipe();
    expect(pipe).toBeTruthy();
  });
});
